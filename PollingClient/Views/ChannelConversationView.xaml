<UserControl x:Class="PollingClient.Views.ChannelConversationView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             Height="460" Width="620">

    <DockPanel Margin="30">
        <TextBlock x:Name="CurrentChannelTextBlock" DockPanel.Dock="Top" Text="Channel" FontSize="24" FontWeight="Bold" Margin="0,0,0,20"/>
        <Button x:Name="LeaveChannelButton" DockPanel.Dock="Bottom" Content="Leave Channel" Height="32" Width="222" Margin="0,10,0,0" Click="LeaveChannelButton_Click"/>
        <ListBox x:Name="MessagesListBox"/>
    </DockPanel>
</UserControl>
