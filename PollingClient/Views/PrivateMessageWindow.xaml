<Window x:Class="PollingClient.Views.PrivateMessageWindow" 
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation" 
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml" 
        Title="Private Message" Height="400" Width="350">
    <Grid Margin="10">
        <Grid.RowDefinitions> <RowDefinition Height="Auto"/> <RowDefinition Height="*"/> <RowDefinition Height="Auto"/> <RowDefinition Height="Auto"/>
        </Grid.RowDefinitions>
        <TextBlock x:Name="HeaderTextBlock" Grid.Row="0" FontWeight="Bold" FontSize="14" Margin="0,0,0,10"/>
        <ListBox x:Name="MessagesListBox" Grid.Row="1"/>
        <TextBox x:Name="ReplyTextBox" Grid.Row="2" Margin="0,10,0,0" KeyDown="ReplyTextBox_KeyDown"/>
        <StackPanel Grid.Row="3" Orientation="Horizontal" HorizontalAlignment="Right" Margin="0,5,0,0">
            <TextBlock x:Name="StatusTextBlock" Foreground="Red" VerticalAlignment="Center" Margin="0,0,10,0"/>
            <Button x:Name="SendButton" Content="Send" Click="SendButton_Click"/>
        </StackPanel>
    </Grid>
</Window>